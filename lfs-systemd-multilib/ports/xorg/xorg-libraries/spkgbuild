# description	: Meta port for Xorg libraries
# depends	: xtrans libx11 libxext libfs libice libsm libxscrnsaver libxt libxmu
# depends	: libxpm libxaw libxfixes libxcomposite libxrender libxcursor
# depends	: libxdamage libfontenc libxfont2 libxft libxi libxinerama 
# depends	: libxrandr libxres libxtst libxv libxvmc libxxf86dga 
# depends	: libxxf86vm libdmx libpciaccess libxkbfile libxshmfence

name=xorg-libraries
version=1.0
release=1


build() {
	mkdir -pv $PKG/etc
}
