#!/bin/bash 
set -e


TMP=${TMP:-/tmp/work}
LOG=${LOG:-$HOME/logs/stage4}
SRC=${SRC:-/sources}

name=python3-sphinxcontrib-serializinghtml
version=1.1.4

mkdir -p $TMP $LOG

tar xf $SRC/sphinxcontrib-serializinghtml-$version.tar.gz -C $TMP

{ time \
   {

    cd $TMP/sphinxcontrib-serializinghtml-$version
    
	python3 setup.py install --optimize=1
    
    }
} 2>&1 | tee $name.log
    
[ $PIPESTATUS = 0 ] && mv $name.log $LOG || exit $PIPESTATUS

rm -fr $TMP
